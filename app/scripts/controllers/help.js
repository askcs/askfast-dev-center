define(
  ['controllers/controllers'],
  function (controllers)
  {
    'use strict';

    controllers.controller ('help',
      [
        '$rootScope', '$scope',
        function ($rootScope, $scope)
        {
        }
      ]
    );
  }
);